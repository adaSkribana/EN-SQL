CREATE TABLE Departamento(
	Numero INT PRIMARY KEY,
	Nombre VARCHAR(100),
	Rut_Gerente VARCHAR(10),
	Gerente_Inicio DATE,
	Ubicacion INT,
	FOREIGN KEY (Ubicacion) REFERENCES empleados(numero_dpto)  
);
